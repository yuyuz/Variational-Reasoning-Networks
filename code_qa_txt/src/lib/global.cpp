#include "global.h"

KnowledgeBase kb;
std::map<std::string, int> side_word_dict;
ParamSet<mode, Dtype> model;
FactorGraph fg;
std::map<std::string, int> word_dict;
std::map<std::string, int> relation_dict;
